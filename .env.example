# 管理后台令牌（用于保护 /payout/run 接口）
ADMIN_TOKEN=change-this-to-a-long-random-string

# 活动 ID —— 必须和前端 index.html 里保持一致
ACTIVITY_ID=mid-autumn-2025

# Solana 网络 RPC —— 可以先用官方的，生产建议用 Helius/QuickNode
SOLANA_RPC=https://api.mainnet-beta.solana.com

# 代币信息（等你发币后填）
SPL_MINT=YOUR_TOKEN_MINT_ADDRESS
SPL_DECIMALS=6

# 金库私钥（Phantom 导出的 Base58 或 keypair.json 数组）
# 切记不要上传真实值到 GitHub！在 Render 的 Environment Variables 填写
SOLANA_TREASURY_SECRET_KEY=your-phantom-secret-key-here

# Firebase 服务账号路径
# 本地调试可放 service-account.json 在根目录
# 在 Render 上传 Secret File，并把这个变量设为对应路径
GOOGLE_APPLICATION_CREDENTIALS=service-account.json

# 批量发放参数（可选）
PAYOUT_BATCH_LIMIT=20
PAYOUT_SLEEP=0.5
