services:
  - type: web
    name: mooncake
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -k gthread -w 2 -b 0.0.0.0:$PORT --timeout 180 --preload
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13

