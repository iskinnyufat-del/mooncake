Flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# Firebase Admin SDK（自带 google-cloud-firestore 依赖）
firebase-admin==6.5.0

# Solana Python SDK（包含 spl.token.* 子模块）
solana==0.30.2

# Render 上用 gunicorn 启动 Flask 应用
gunicorn==21.2.0


